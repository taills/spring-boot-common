name: 主分支推送😄
on:
  push:
    branches:
      - main
jobs:
  build-and-publsh:
    runs-on: ubuntu-20.04
    steps:
      - name: 启动 MySQL 服务
        run: sudo systemctl start mysql.service

      - name: 配置环境变量
        run: |
          echo "::set-env name=DB_HOST::localhost"
          echo "::set-env name=DB_PORT::3306"
          echo "::set-env name=DB_USER::root"
          echo "::set-env name=DB_PASSWORD::root"
          echo "::set-env name=DB_NAME::taills"

      - name: 创建数据库
        run: |
          mysql -u${DB_USER} -p${DB_PASSWORD} -e "create database ${DB_NAME}"
          mysql -u${DB_USER} -p${DB_PASSWORD} -e "show databases"

      - name: Check out Git repository
        uses: actions/checkout@v2

      - name: Set up Apache Maven Central
        uses: actions/setup-java@v2
        with:
          distribution: 'zulu'
          java-version: '8'
          cache: 'maven'
          server-id: ossrh
          server-username: NEXUS_USERNAME # env variable for username in deploy
          server-password: NEXUS_PASSWORD # env variable for token in deploy
          gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }} # Value of the GPG private key to import
          gpg-passphrase: GPG_PASSPHRASE # env variable for GPG private key passphrase

      - name: Publish to Apache Maven Central
        run: mvn clean install deploy
        env:
          NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
          NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}